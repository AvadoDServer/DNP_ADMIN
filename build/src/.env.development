# Loaded by Vite ONLY in dev (`yarn dev` / `vite`), never in a production build.
# Drives the offline mock-data path in src/index.jsx so the Admin UI can be
# developed and previewed without a live AVADO box / WAMP backend.
REACT_APP_MOCK_DATA=true
